<Window x:Class="LoginTrailTest.AdminRoute.CreateRoute"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginTrailTest.AdminRoute"
        mc:Ignorable="d"
        Title="CreateRoute" Height="450" Width="350" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FACA87" Padding="10">
            <TextBlock Text="Creating a path" Foreground="White" FontSize="20" HorizontalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <!-- Add your input controls here -->
            <StackPanel>
                <TextBox x:Name="TrainIDTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
                <TextBox x:Name="StationNameTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
                <TextBox x:Name="RouteLengthTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
                <TextBox x:Name="PriceCoupeTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
                <TextBox x:Name="PricePlacTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Create" Width="100" Margin="5" Click="CreateButton"/>
            <Button Content="Cancel" Width="100" Margin="5" Click="CancelButton"/>
        </StackPanel>
    </Grid>
</Window>
