<Window x:Class="LoginTrailTest.AdminRoute.SearchRoute"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginTrailTest.AdminRoute"
        mc:Ignorable="d"
        Title="SearchRoute" Height="450" Width="900" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FACA87" Padding="10">
            <TextBlock Text="Search for a way" Foreground="White" FontSize="20" HorizontalAlignment="Center"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <!-- Add your search controls here -->
            <StackPanel>
                <TextBox x:Name="SearchTextBox" Margin="5" Width="200" VerticalAlignment="Center"/>
                <Button Content="Search" Width="100" Margin="5" HorizontalAlignment="Center" Click="SearchButton"/>
            </StackPanel>
        </Grid>

        <!-- Result ListView -->
        <ListView x:Name="YourListView" Grid.Row="2" Margin="20">
            <!-- Optional: you can specify a style for the ListView -->
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="RouteID" DisplayMemberBinding="{Binding Route_ID}" Width="120"/>
                    <GridViewColumn Header="TrainNumber" DisplayMemberBinding="{Binding Train_Number}" Width="120"/>
                    <GridViewColumn Header="StationName" DisplayMemberBinding="{Binding Station_Name}" Width="120"/>
                    <GridViewColumn Header="Route Length" DisplayMemberBinding="{Binding Route_Length}" Width="120"/>
                    <GridViewColumn Header="Price Coupe" DisplayMemberBinding="{Binding Price_coupe}" Width="120"/>
                    <GridViewColumn Header="Price Plac" DisplayMemberBinding="{Binding Price_plac}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
