<Window x:Class="LoginTrailTest.TrainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:LoginTrailTest"
        mc:Ignorable="d"
        Title="TrainWindow" Height="800" Width="1200" Background="Transparent" ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <!-- Up panel -->
        <Border BorderThickness="0 0 0 2" BorderBrush="#e2e9e9" CornerRadius="10 10 0 0" Background="LightSkyBlue" Padding="10"
        MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_MouseDown">
            <Grid>
                <Icon:PackIconMaterial Kind="Train" HorizontalAlignment="Center"/>
                
                <Button Click="AccountButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Left">
                    <Icon:PackIconMaterial Kind="Account" HorizontalAlignment="Left"/>
                </Button>
                <Button Click="ExitButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right">
                    <Icon:PackIconMaterial Kind="ExitRun" HorizontalAlignment="Right"/>
                </Button>
            </Grid>
        </Border>

        <!-- Down panel -->
        <Border Grid.Row="2" BorderThickness="0 2 0 0" BorderBrush="#e2e9e9" CornerRadius="0 0 10 10" Padding="20" Background="LightSkyBlue">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="TrainButton_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="Train"/>
                    </Button.Content>
                </Button>

                <Button Grid.Column="1" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="SeatButton_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="SeatPassenger"/>
                    </Button.Content>
                </Button>

                <Button Grid.Column="2" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="RouteButton_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="RoadVariant"/>
                    </Button.Content>
                </Button>

                <Button Grid.Column="3" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="CalendarButton_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="Calendar"/>
                    </Button.Content>
                </Button>

                <Button Grid.Column="4" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="MapMarkerPath_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="CityVariant"/>
                    </Button.Content>
                </Button>

                <Button Grid.Column="5" Width="30" Margin="5" Background="{x:Null}" BorderBrush="{x:Null}" Click="TicketAccount_Click">
                    <Button.Content>
                        <Icon:PackIconMaterial Kind="TicketAccount"/>
                    </Button.Content>
                </Button>
            </Grid>
        </Border>

        <!-- Middle panel -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.Background>
                <SolidColorBrush Color="#F2F2F0"/>
            </Grid.Background>

            <!-- StackPanel для розміщення кнопок зверху -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Create" Height="35" Width="200" Margin="5" Click="CreateButton_Click"/>
                <Button Content="Edit" Height="35" Width="200" Margin="5" Click="EditButton_Click"/>
                <Button Content="Remove" Height="35" Width="200" Margin="5" Click="DeleteButton_Click"/>
                <Button Content="Search" Height="35" Width="200" Margin="5" Click="SearchButton_Click"/>
            </StackPanel>

            <!-- Grid для розміщення таблиці Train -->
            <Grid Grid.Row="1" Margin="20">
                <DockPanel>
                    <ListView Name="YourData" FontSize="18" FontFamily="Arial">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Identifier" DisplayMemberBinding="{Binding Train_ID}" Width="100"/>
                                <GridViewColumn Header="Train number" DisplayMemberBinding="{Binding Train_Number}" Width="150"/>
                                <GridViewColumn Header="Departure station" DisplayMemberBinding="{Binding Departure_Station}" Width="150"/>
                                <GridViewColumn Header="Destination station" DisplayMemberBinding="{Binding Destination_Station}" Width="160"/>
                                <GridViewColumn Header="Number of cars" DisplayMemberBinding="{Binding Number_of_Cars}" Width="150"/>
                                <GridViewColumn Header="Types of railcars" DisplayMemberBinding="{Binding Car_Types}" Width="140"/>
                                <GridViewColumn Header="Coupe" DisplayMemberBinding="{Binding Number_of_Coupe}" Width="140"/>
                                <GridViewColumn Header="Reserved seat" DisplayMemberBinding="{Binding Number_of_Plac}" Width="140"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
